import apiSlice from './apiSlice';
import productsSlice from './productsSlice';
import cartSlice from './cartSlice';

const slices = { api: apiSlice, products: productsSlice, cart: cartSlice };

export default slices;
